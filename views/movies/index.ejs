<% include ../partials/header.ejs %>

<div class='container white'>
  <div class='row'>
    <h1>Movie List</h1>
    <ul class='movies'> 
      <% movies.forEach(function(movie) { %>
        <li class='card'>
          <div class='card-content'>
            <a href="<%= '/movies/' + movie.id %>">
              <%= movie.title %>
            </a>
            <% if (user !== undefined) { %>
              <div class='card-action'>
                <form action="<%= `/movies/${movie.id}/${user.id}` %>" method="POST">
                  <button type='submit' class='actioner btn' data-movieid='<%= movie.id %>'><i class='fa fa-heart fa-2x'></i></button>
                </form>
              </div>
            <% } %>
          </div>
        </li>
      <% }) %>
    </ul>
  </div>
</div>

<button type="submit"><a href="/movies/add">Add Movie</a></button>

<% include ../partials/footer.ejs %>
